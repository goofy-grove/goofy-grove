[package]
name = "gg_core"
version = "0.1.0"
edition = "2024"

[lib]
name = "gg_core"
path = "src/lib.rs"

[dependencies]
thiserror = "2.0.18"

[dev-dependencies]
tokio = { version = "1.49.0", features = ["macros", "rt-multi-thread"] }
